<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.meglio.albuquerk.rubaink.devilpass">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_ticket_sq"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_ticket_ho"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NextRegActivity"></activity>
        <activity android:name=".ModifyPActivity" />
        <activity android:name=".SetupUActivity" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity
            android:name=".XcanActivity"
            android:theme="@style/AppFullScreenTheme" />
        <activity android:name=".MyPassActivity" />
        <activity
            android:name=".WelActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppFullScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".TicketActivity" />
        <activity android:name=".ForgetActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" /> <!-- Enlever le Jaune -->
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity android:name=".LunchActivity" />
        <activity android:name=".WelcomeActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SetupActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".AboutActivity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".FuturActivity" />
        <activity android:name=".SelectGroupActivity" />
        <activity android:name=".SelectInfoActivity" />
        <activity android:name=".SelectImageActivity" />
        <activity
            android:name=".SelectLocatActivity"
            android:label="@string/title_activity_select_locat" />
        <activity android:name=".SelectTicketActivity" />
        <activity android:name=".GroupEventActivity" />
        <activity android:name=".TrensitionActivity" />
        <activity android:name=".SecondActivity" />
        <activity android:name=".SecondImageActivity" />
        <activity android:name=".SecondLocActivity" />
        <activity android:name=".SecondInfoActivity" />
        <activity android:name=".PostsActivity" />
        <activity android:name=".SecondTicketActivity" />
        <activity
            android:name=".ScrollingActivity"
            android:label="@string/title_activity_scrolling"
            android:theme="@style/AppTheme" />
        <activity android:name=".ConeActivity" />
        <activity android:name=".WebActivity" />
        <activity android:name=".UserEvActivity" />
        <activity android:name=".EventLocActivity" />
        <activity android:name=".ReserverActivity" />
        <activity
            android:name=".Scrolling2Activity"
            android:label="@string/title_activity_scrolling2"
            android:theme="@style/AppTheme" />
    </application>

</manifest>